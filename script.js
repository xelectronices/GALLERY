const menu=document.querySelector(".menu-toggle"),nav=document.querySelector(".nav");if(menu){menu.addEventListener("click",()=>{const active=nav.classList.toggle("active");menu.setAttribute("aria-expanded",active)});document.querySelectorAll(".nav a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("active");menu.setAttribute("aria-expanded","false")}))}document.getElementById("year").textContent=new Date().getFullYear();const obs=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("show");obs.unobserve(e.target)}})},{threshold:.08});document.querySelectorAll(".card,.photo,.review,.trust-photo,.hero-copy,.hero-photo").forEach(el=>{el.classList.add("reveal");obs.observe(el)});